# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Frontend (root-level safety net; frontend/ has its own .gitignore once scaffolded)
node_modules/
dist/

# Local env files at project root (each app has its own .env handled in subdir .gitignore)
.env
.env.local
